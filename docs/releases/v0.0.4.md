## Fixes

- Block unsafe Gemini session reloads after restart so old replies cannot mix into a new response. Saved sessions and accepted messages are preserved; use Gemini CLI to continue or `/new` for fresh context.
- Require passing main CI before publishing and verify npm serves the tested package.
- Preserve existing GitHub releases during recovery and use reviewed release notes.
- Remove a timing race from the development-server restart test.

**Full changelog:** [v0.0.3...v0.0.4](https://github.com/commonspaceai/commonspace/compare/v0.0.3...v0.0.4)
